# Production environment settings (RunPod/Docker)
# We leave these empty so the app defaults to relative URLs through the Nginx reverse proxy.
# This prevents the app from trying to connect to 'localhost' in the user's browser.

VITE_COMFY_URL=/comfy
VITE_BACKEND_URL=
VITE_COMFY_WS_URL=
